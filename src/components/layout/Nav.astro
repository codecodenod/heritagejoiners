---
/**
 * Nav - primary navigation
 */
import { siteConfig } from '../../config/site';

interface Props {
  mobile?: boolean;
}

const props = Astro.props;
const isMobile = props.mobile || false;

const navLinks = [
  { href: '/services', label: 'Services' },
  { href: '/gallery', label: 'Gallery' },
  { href: '/about', label: 'About' },
  { href: '/case-studies', label: 'Case Studies' },
  { href: '/blog', label: 'Blog' },
  { href: '/areas', label: 'Areas' },
  { href: '/contact', label: 'Contact' },
];
---

{isMobile ? (
  <nav class="flex flex-col gap-2 text-lg">
    {navLinks.map((link) => (
      <a
        href={link.href}
        class="font-display text-ink hover:text-oak transition-colors py-3 border-b border-ink/10"
      >
        {link.label}
      </a>
    ))}
  </nav>
) : (
  <nav class="hidden lg:flex items-center gap-x-3 xl:gap-x-5">
    {navLinks.map((link) => (
      <a
        href={link.href}
        class="font-display text-sm text-ink hover:text-oak transition-colors whitespace-nowrap"
      >
        {link.label}
      </a>
    ))}
  </nav>
)}
